# X-Ray Hourly

切换到 /Users/nocoo/workspace/personal/xray 目录，执行 x-ray-pipeline skill。

完成后，用语音播报结果：

```bash
osascript -e 'do shell script "say -v Tingting X分析完成，共筛选了N条值得关注的内容"'
```

将 N 替换为实际筛选的推文数量。
