name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  swift-tests:
    uses: nocoo/base-ci/.github/workflows/test-job.yml@ad43150de3a2be2fa464b5cd2f921dc4fa9f8f0f
    with:
      runner: "macos-latest"
      runtime: "none"
      command: "cd runner-swift && swift test --no-parallel"

  dashboard-quality:
    uses: nocoo/base-ci/.github/workflows/quality.yml@ad43150de3a2be2fa464b5cd2f921dc4fa9f8f0f
    with:
      runtime-version: "1.4.2"
      install-policy: "blocked"
      working-directory: "dashboard"
      test-command: "bun run test"
      lint-command: "bun run lint"
      typecheck-command: "bun run typecheck"
      security: false
      coverage-path: ""
      disabled-check-reasons: '{"security": "security scans run at repository root in separate security job"}'

  security:
    uses: nocoo/base-ci/.github/workflows/security.yml@ad43150de3a2be2fa464b5cd2f921dc4fa9f8f0f
    with:
      lockfiles: "bun.lock,dashboard/bun.lock"
      osv-config: "osv-scanner.toml"
      gitleaks-config: ".gitleaks.toml"
